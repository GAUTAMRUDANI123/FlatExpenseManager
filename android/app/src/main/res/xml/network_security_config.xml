<?xml version="1.0" encoding="utf-8"?>
<!--
  The app talks to the API over HTTPS. In normal use that is the Cloudflare
  Tunnel URL, which is HTTPS end to end and needs no exemption here.

  The exemptions below exist only so the app can be run against an API on the
  development machine, where there is no certificate. They are exact IP
  literals on purpose: Android matches <domain> entries literally and has no
  CIDR support, so a subnet like 192.168.1.0/24 cannot be expressed here at
  all. An earlier version listed "192.168.0.0" and "192.168.1.0" with
  includeSubdomains, which matched neither — includeSubdomains applies to DNS
  names, not address ranges — so every real LAN address was refused.

  To point a physical phone at a laptop over plain HTTP, add that laptop's
  exact address as its own <domain> line. Using the tunnel instead avoids the
  problem entirely, and is what SETUP.md describes.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <!-- How the Android emulator reaches the host machine. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
